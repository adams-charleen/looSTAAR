% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/looSTAAR.R
\name{plot_looSTAAR}
\alias{plot_looSTAAR}
\title{Plot LOO STAAR-O Influence}
\usage{
plot_looSTAAR(
  results,
  variant_info = NULL,
  title = "Leave-One-Out Influence on STAAR-O",
  ylim = c(-1.2, 1.2)
)
}
\arguments{
\item{results}{Data.frame with columns: Variant_ID, Delta_log10p, Position}

\item{variant_info}{Optional data.frame with Variant_ID and Position (if not in results)}

\item{title}{Plot title (default: "Leave-One-Out Influence on STAAR-O")}

\item{ylim}{Y-axis limits (default: c(-1.2, 1.2))}
}
\value{
ggplot object
}
\description{
Creates a scatter plot of Delta_log10p against genomic position, highlighting
driver and diluter variants.
}
\examples{
\dontrun{
if (requireNamespace("ggplot2", quietly = TRUE) && requireNamespace("ggrepel", quietly = TRUE)) {
  results <- data.frame(Variant_ID = paste0("var", 1:100), Delta_log10p = runif(100, -0.01, 0.01))
  results$Position <- 1:100
  plot_looSTAAR(results)
}
}
}
